# STARTER-P01 — Repository + Documentation + App Skeleton

## Main result

A minimal React + TanStack Start Full-Stack + TypeScript repository skeleton with TPES project documents.

## Scope boundary

No PostgreSQL connection, migrations, Auth, Authorization, First Admin, or Product-specific domain implementation.

## PASS gate

- dependencies install;
- development server starts;
- TypeScript check passes;
- production build passes;
- `.env.example` has placeholders only;
- no secret is committed;
- required project documents exist;
- no Product-specific domain is introduced.
